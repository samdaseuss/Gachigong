{{>layouts/header}}
<form class="container" action=/group/{{groupId}} method="POST">
    <div class="mb-3">{{groupName}}</div>
    <div class="mb-3">{{groupIntro}}</div>
    <div class="mb-3">
        <label class="form-label">비밀번호</label>
        <input type="password" class="form-control" name="groupPassword">
    </div>
    <button type="submit" class="btn btn-primary">입장하기</button>
</form>
{{>layouts/footer}}